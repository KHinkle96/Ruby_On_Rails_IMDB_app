<% @games.each do |game| %>
    <p>
      <%= game.title %>
      -
      <%= link_to("edit", edit_game_path(game.id)) %>
      -
      <%= link_to("delete", game_path(game.id), method: :delete, data: {confirm: "Are you really sure you want to do this????????"}) %>
    </p>
  <% end %>